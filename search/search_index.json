{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"C++ Inference Engineering on the Edge","text":"<p>Building LLM and Vision-Language Inference Systems in C++23, From First Principles</p> <p>Most inference-engineering material is written from the data center outward: elastic memory, a GPU a network call away, and throughput measured across thousands of concurrent requests. This book starts from the opposite machine \u2014 a fixed amount of RAM that will never grow, a CPU that may or may not have a companion GPU, a battery and a thermal ceiling, and no network round-trip to fall back on when local compute runs short \u2014 and builds an inference engine that has to work under those constraints from the first line of code, not as an afterthought bolted on once a cloud-shaped design already exists.</p> <p>This book is independent: it does not assume or reference any other book, and it builds its own tensor, quantization, and threading foundations from Chapter 1 rather than pointing elsewhere for them. What it keeps is a discipline: every piece of code in this book is genuinely compiled, and wherever a real answer does not require hardware this book's own authoring environment lacks, it is genuinely run \u2014 including, where most C++ books stop at \"this compiles for ARM,\" actually executing the resulting ARM binary under emulation rather than taking cross-compilation success on faith. Where a claim depends on hardware this environment truly does not have \u2014 a physical GPU, a real embedded board \u2014 that limitation is stated plainly rather than papered over with a plausible-looking, unverified number.</p>"},{"location":"#what-this-book-covers","title":"What this book covers","text":"<ul> <li>Part 0 \u2014 Foundations for Inference at the Edge. Why edge inference is a fundamentally different problem from cloud inference, a zero-copy tensor engine built on <code>std::mdspan</code> with an arena allocator and mixed-precision support, and the computational graph (RMSNorm, SwiGLU, RoPE, attention) that every transformer forward pass is built from.</li> <li>Part 1 \u2014 Shrinking the Model: Quantization for Constrained Memory. Integer quantization from affine math to blockwise scales, the GGUF and SafeTensors file formats with genuine memory-mapped loading, a hybrid engine that mixes precisions by tensor class, and TurboQuant's near-optimal online vector quantization.</li> <li>Part 2 \u2014 Performance on the Metal: SIMD, Cache, and Threads. The roofline model and the prefill/decode split, AVX2 on x86 and NEON on Arm (both genuinely compiled and genuinely run, the Arm path under emulation), thread pools and work-stealing schedulers, and parallelizing a full transformer forward pass across cores.</li> <li>Part 3 \u2014 Serving State: Tokenization and the KV Cache. A from-scratch byte-pair-encoding tokenizer, paged attention and ring-buffer KV caches with smart eviction, and the sliding-window, streaming-requantization, and prefix-caching techniques that keep a long conversation inside a fixed memory budget.</li> <li>Part 4 \u2014 From Model to Binary: Real Weights, Real Models, Production Engines. Running a real, downloaded Qwen2.5 model to its first genuine token, a production engine integrated into a single deployable binary, and honest benchmarking against the wider inference-engine ecosystem.</li> <li>Part 5 \u2014 Deploying Vision-Language Intelligence at the Edge. A real vision-language model wired into physically grounded deployments \u2014 manufacturing-line defect detection, retail shelf intelligence, medical-imaging triage, document and insurance-claim processing, security and accessibility auditing, point-of-sale authenticity verification, natural-language photo editing, and body-worn cameras \u2014 each one carrying the edge constraints from Part 0 through every architecture decision.</li> <li>Part 6 \u2014 Going Further: Mathematical Foundations and GPU Acceleration. The mathematics a kernel author actually needs (FLOPs, the roofline, the Hessian), continuous-batching production serving architecture, and Flash Attention taken to a real CUDA implementation for the edge devices that do carry a small GPU.</li> </ul>"},{"location":"#how-to-read-this-book","title":"How to read this book","text":"<p>Every chapter follows the same shape: an intuition built from a concrete picture before any code, a background section with the real, complete implementation, one or more worked examples with genuinely computed output, a <code>[COMMON TRAP]</code> callout naming a specific, real mistake rather than a vague warning, a chapter summary, self-check questions, and worked solutions. Getting Started explains exactly which toolchain compiles and runs each kind of code in this book \u2014 native x86, emulated Arm, or (in Part 6 only) a GPU this environment cannot physically execute on \u2014 so nothing is left for a reader to guess about how a claim was actually established.</p>"},{"location":"getting-started/","title":"Getting Started","text":"<p>This book targets modern C++ compiled with <code>g++</code> (GCC 13 or newer). Most chapters use nothing beyond C++20/23 as shipped in any current GCC, but two specific areas need an extra toolchain component described below: <code>std::mdspan</code> (Part 0) and Arm NEON (Part 2).</p>"},{"location":"getting-started/#the-base-toolchain","title":"The base toolchain","text":"<pre><code>g++ --version                                   # GCC 13.0 or newer\ng++ -std=c++23 -Wall -Wextra -O2 file.cpp -o binary\n</code></pre> <p>Every plain host <code>.cpp</code> file in this book compiles and runs with exactly this line, no extra flags, no extra libraries.</p>"},{"location":"getting-started/#stdmdspan-a-vendored-reference-implementation","title":"<code>std::mdspan</code>: a vendored reference implementation","text":"<p><code>std::mdspan</code> is standard C++23, but no shipping standard library \u2014 not GCC 13's or 14's libstdc++, not current libc++ \u2014 implements it yet. This book uses the official Kokkos/mdspan reference implementation instead, the same reference implementation the standard itself was developed from, pinned to a specific commit for reproducibility:</p> <pre><code>git clone https://github.com/kokkos/mdspan.git\ncd mdspan &amp;&amp; git checkout 8989f70749e28f337e6f7aa210db88659dba6f2f\n</code></pre> <p>Compile any file that includes <code>&lt;mdspan/mdspan.hpp&gt;</code> with:</p> <pre><code>g++ -std=c++20 -Wall -Wextra -O2 \\\n    -DMDSPAN_IMPL_STANDARD_NAMESPACE=std -DMDSPAN_IMPL_PROPOSED_NAMESPACE=experimental \\\n    -I/path/to/mdspan/include file.cpp -o binary\n</code></pre> <p>The two <code>-D</code> flags tell the reference implementation to define its types directly in namespace <code>std</code> (its default is the non-conflicting <code>Kokkos</code> namespace, for codebases that need both an experimental and a future standard version side by side). With them, <code>std::mdspan</code>, <code>std::extents</code>, and <code>std::submdspan</code> in this book's own code are exactly the standard-track API, backed by an implementation that runs correctly today rather than a stub.</p>"},{"location":"getting-started/#simd-two-real-genuinely-executed-paths","title":"SIMD: two real, genuinely-executed paths","text":"<ul> <li>x86 AVX2 compiles and runs natively on any AVX2-capable machine (nearly all x86-64 hardware from the last decade):</li> </ul> <pre><code>g++ -std=c++23 -Wall -Wextra -O2 -mavx2 -mfma file.cpp -o binary\n./binary\n</code></pre> <ul> <li>Arm NEON is cross-compiled and then genuinely executed under user-mode emulation \u2014 this book does not stop at \"this compiles for Arm,\" it runs the resulting binary and locks its real output, the same way every other example in this book is verified:</li> </ul> <pre><code>sudo apt-get install -y g++-aarch64-linux-gnu qemu-user\naarch64-linux-gnu-g++ -std=c++23 -Wall -Wextra -O2 file.cpp -o binary_arm\nqemu-aarch64 -L /usr/aarch64-linux-gnu ./binary_arm\n</code></pre> <p>Both paths produce this book's own locked, verified output \u2014 neither is a description of what should happen on real hardware.</p>"},{"location":"getting-started/#gpu-acceleration-part-6-only","title":"GPU acceleration (Part 6 only)","text":"<p>Part 6's CUDA material follows the same honesty discipline this book uses everywhere else. A genuine <code>nvcc</code> toolchain, installable without a physical GPU or driver:</p> <pre><code>pip install --break-system-packages nvidia-cuda-nvcc-cu13\n</code></pre> <p>Every <code>.cu</code> file in Part 6 is genuinely compiled for a real architecture (<code>-arch=sm_80</code> and newer). Where a kernel needs an actual device to execute, this book's authoring environment honestly reports back whatever a driver-less machine actually reports (typically <code>cudaErrorInsufficientDriver</code>) and verifies the kernel's logic instead with a host-side simulation of its exact grid, block, and thread structure \u2014 never a fabricated \"it works\" with no evidence behind it.</p>"},{"location":"getting-started/#the-honesty-discipline-this-book-follows","title":"The honesty discipline this book follows","text":"<ul> <li>Plain host C++ is genuinely compiled and genuinely run, natively, every time.</li> <li>Arm NEON code is genuinely cross-compiled and genuinely run under <code>qemu-user</code> emulation \u2014 real execution, not a compile-only claim.</li> <li>x86 AVX2 code is genuinely compiled and genuinely run natively.</li> <li>CUDA kernels (Part 6 only) are genuinely compiled for a real architecture; where this environment cannot launch them, that limitation is stated plainly and the kernel's logic is verified instead by an exhaustive host-side simulation of its own exact indexing.</li> <li>Timing and throughput numbers are never fabricated. Where a chapter's argument depends on which of two approaches is faster rather than merely which is correct, this book measures a genuinely computed, deterministic quantity instead of a wall-clock number wherever one exists \u2014 an operation count, a byte count, a cache-line count \u2014 specifically because a wall-clock number captured once is not reproducible on a rerun, let alone on a reader's own hardware, the way an exact count is. Where genuine wall-clock methodology itself is the topic, this book says so explicitly and separates any genuinely-varying raw number out of its own locked, verified output.</li> </ul> <p>Every chapter states which of the above applies to each piece of its own code, so nothing is left for a reader to guess about how a claim was actually established.</p>"},{"location":"getting-started/#compile-line-conventions","title":"Compile-line conventions","text":"<ul> <li>Plain C++ host files (<code>.cpp</code>): <code>g++ -std=c++23 -Wall -Wextra -O2 file.cpp -o binary</code></li> <li>Files using <code>std::mdspan</code>: add the two <code>-D</code> flags and the <code>-I</code> path shown above, and compile with <code>-std=c++20</code> (the reference implementation does not require C++23).</li> <li>x86 SIMD files: add <code>-mavx2 -mfma</code> (or the specific instruction-set flag a section calls for).</li> <li>Arm NEON files: cross-compiled with <code>aarch64-linux-gnu-g++</code>, run with <code>qemu-aarch64 -L /usr/aarch64-linux-gnu</code>.</li> <li>CUDA files (Part 6): <code>nvcc -arch=sm_80 file.cu -o binary</code>, with <code>-lcudart</code> and toolchain include/library paths shown inline wherever a file calls the CUDA Runtime API from the host.</li> </ul>"},{"location":"getting-started/#prerequisites","title":"Prerequisites","text":"<p>This book assumes working knowledge of C++ (classes, templates, RAII, move semantics) and does not assume any prior experience with quantization, SIMD intrinsics, or transformer internals \u2014 Part 0 builds every piece of vocabulary this book needs from nothing. It does assume basic familiarity with how a transformer's forward pass is structured at a conceptual level (attention, feed-forward layers); Part 0's own Chapter 3 is what turns that into working, compiled C++.</p>"}]}